---
ticket: KOEA-2461
planning_issue: KOEA-2516
planner: planner
date: 2026-05-14
estimated_complexity: medium
estimated_token_cost: "$0.45"
repo: learnovaBeast/learnova-academy
base_branch: academy/redesign-v1
basebranch_verified: true
chain_alert_unblocked_by: f06a3e59-425d-4302-9b81-c1b6f0a46759
revised_for_issue: KOEA-2518
revision_note: "Use isolated node-owned implementation checkout after host-root path drift was identified."
---

# Plan: Fix Claude tool-use course chapter metadata rendering

## Goal
Fix `/learn/claude-tool-use-from-zero` so it no longer renders metadata sidecar files as chapters, no longer shows duplicate chapter numbers or `0m` chapter rows from non-chapter files, and emits anchors that match the links generated by course content and SEO metadata. Success is observable by comparing the Claude course page with `/learn/mcp-from-first-principles-to-production`: visible chapter rows should be content-bearing chapters only, counts should describe rendered chapters rather than stale outline promises, and all chapter jump links should resolve.

## Context
- Files to read first: `learnova-academy/src/lib/courses.ts:152-224`, `learnova-academy/src/app/learn/[slug]/page.tsx:90-95`, `learnova-academy/src/app/learn/[slug]/page.tsx:183-188`, `learnova-academy/src/app/learn/[slug]/page.tsx:266-331`, `learnova-academy/src/app/learn/[slug]/page.tsx:416-448`, `learnova-academy/src/app/learn/[slug]/page.tsx:1256-1285`, `learnova-academy/src/lib/seo.ts:64-76`, `learnova-academy/scripts/sync-vault.mjs:102-171`.
- Content inputs to inspect: `vault/courses/claude-tool-use-from-zero/outline.md`, `vault/courses/claude-tool-use-from-zero/*.md`, `vault/courses/mcp-from-first-principles-to-production/outline.md`, `vault/courses/mcp-from-first-principles-to-production/*.md`, plus mirrored media under `learnova-academy/public/courses/<slug>/`.
- Root cause found: `readCourseOutline()` currently accepts every filename matching `/^\d+-[a-z0-9-]+\.md$/`, so sidecars such as `06-security-and-authentication-meta.md`, `07-creative-connectors-meta.md`, and `08-legal-connectors-meta.md` become chapters. `readChapter()` then falls back to `title: data.title ?? file` and `duration_min: data.duration_min ?? 0`, producing raw filenames and `0m` rows. The Claude vault folder also has no real chapter markdown for chapters 1-6 or 9 in current git history; the outline advertises `chapter_count: 9`, so the page presents an incomplete course as if it were complete.
- Anchor root cause: `chapterAnchor()` emits `ch-<padded chapter number>-<slugified title>`, while wikilinks and schema URLs can point at `#ch-<chapter-slug>` or `#ch-<N>`, so generated links can miss the rendered section ids.
- Relevant prior work: current vault history only shows actual Claude chapter sources for 7 and 8, plus metadata/asset files for 6, 7, 8, and 9; no historical full chapter files for 1-6 or 9 were found by `git log -- vault/courses/claude-tool-use-from-zero`.
- Constraints: use the isolated node-owned implementation checkout `/paperclip/instances/default/workspaces/learnovaBeast-KOEA-2518` and base it on `academy/redesign-v1`; do not use the host-root `/Users/vardaankoenig/Documents/Paperclip/learnovaBeast-fe-agent` path, and do not use the shared `/paperclip/instances/default/workspaces/learnovaBeast-fe-agent` checkout because it is locked for KOEA-2423. Do not mutate the dirty `/Users/vardaankoenig/Documents/Paperclip/learnovaBeast` checkout. Do not run a Convex deploy. Any production deploy, if later approved, must originate from `learnova-tc`.

## Approach (1 chosen, alternatives rejected)
**Chosen**: Harden course ingestion and render only valid chapter data. Update the academy reader so metadata sidecars are never chapter candidates, chapter titles fall back to the first H1 before the filename, durations can fall back to `reading_time_min` when `duration_min` is absent, and outline `chapter_count` does not override the number of renderable chapters when the source folder is incomplete. Update chapter anchors so the canonical id is based on the chapter slug and add/maintain aliases for numeric `#ch-N` links. This fixes the broken page without inventing missing long-form chapter content.

**Rejected**: Content-only cleanup of the Claude vault folder; it would still leave the reader vulnerable to future `*-meta.md` sidecars and cannot restore chapters 1-6/9 because they are not present in current git history. **Rejected**: Re-author missing chapters 1-6 and 9 as part of this ticket; that is content production, far larger than a metadata/rendering repair, and should be a separate Content Author task if Chief Engineering wants the full course completed.

## Steps (Executor follows in order)
1. Prepare `/paperclip/instances/default/workspaces/learnovaBeast-KOEA-2518` as an isolated execution worktree on `academy/redesign-v1`, confirm `git status --short` is clean before edits, respect the repo `.claude/agent-lock` requirement before touching files, and keep all code changes inside `learnova-academy/`.
2. In `learnova-academy/src/lib/courses.ts`, replace the loose chapter filename filter with a helper that excludes sidecars (`*-meta.md`, `*-meta-meta.md`, metadata-only files, and chapter asset directories) and keeps only content-bearing `NN-*.md` chapter sources.
3. In `learnova-academy/src/lib/courses.ts`, update `readChapter()` to derive `slug` from the content filename, use `data.title` then first Markdown H1 before falling back to the filename, and use `data.duration_min` then `data.reading_time_min` before falling back to `0`; do not silently accept metadata-only files as publishable chapters.
4. In `learnova-academy/src/lib/courses.ts` and `learnova-academy/src/app/learn/[slug]/page.tsx`, make displayed chapter count and FAQ/schema count reflect `course.chapters.length` when the outline count disagrees with valid parsed chapters; this prevents `chapter_count: 9` from claiming a complete course when only valid chapter markdown is present.
5. In `learnova-academy/src/app/learn/[slug]/page.tsx` and `learnova-academy/src/lib/seo.ts`, standardize chapter anchors around the parsed chapter slug, and preserve numeric aliases for existing `#ch-6`, `#ch-7`, and padded `#ch-07` style links so in-page chapter links and structured-data URLs resolve to actual DOM ids.
6. Add the smallest regression coverage available in this repo: either a lightweight Node/TypeScript fixture check for `getCourse("claude-tool-use-from-zero")` and `getCourse("mcp-from-first-principles-to-production")`, or, if no test harness is practical, a committed validation script under `learnova-academy/scripts/` that fails when parsed chapters include `*-meta` files, duplicate `chapter_num` values, raw `.md` titles, or zero-minute visible chapters.
7. Leave the vault content mostly untouched except for metadata corrections needed for already-existing real chapters, such as adding `chapter_num`, `title`, and `duration_min` to `vault/courses/claude-tool-use-from-zero/07-creative-connectors.md` if the reader fallback is not sufficient. Do not author missing full chapters in this implementation task.

## Verification (QA Verifier checks these)
- [ ] From `learnova-academy/`, run `pnpm typecheck` and `pnpm build` using `node node_modules/next/dist/bin/next build` if the local Vite/Next wrapper hangs on this filesystem.
- [ ] Run the new course validation check, or the equivalent one-off command, and confirm both target courses parse without metadata sidecars, duplicate chapter numbers, raw `.md` titles, or zero-minute visible chapter rows.
- [ ] Browser walkthrough: start the academy locally, open `/learn/claude-tool-use-from-zero`, and confirm no visible chapter row is named like `07-creative-connectors.md` or `*-meta.md`, no row shows `0m`, the header count matches the rendered rows, and every top chapter link scrolls to a real section.
- [ ] Browser comparison: open `/learn/mcp-from-first-principles-to-production` and confirm its chapter rows still render as chapters 1-5 with titles, non-zero durations, media affordances, and working anchors.
- [ ] Direct anchor checks: manually visit `/learn/claude-tool-use-from-zero#ch-7`, `/learn/claude-tool-use-from-zero#ch-07`, and `/learn/claude-tool-use-from-zero#ch-creative-connectors` or the final canonical slug form chosen by the implementation; each supported alias should land on the intended chapter or redirect/scroll equivalently.

## Risk
- Risk: Filtering sidecar files globally may hide a real chapter if an old course used `-meta` in a legitimate chapter slug. Mitigation: validate all existing `vault/courses/*` folders and, if a real chapter collides with the sidecar naming rule, rename that chapter source explicitly rather than weakening the filter.

## Out of scope
- Writing or reconstructing missing long-form Claude chapters 1-6 and 9.
- Publishing or deploying from this ticket; no Convex deploy is required.
- Changing the course authoring pipeline beyond the minimal validation needed to stop metadata sidecars from rendering as chapters.
